---
title: Project Structures
decription: Blueprint for efficient and logical project organization 🫚
---

import { FileIcon } from '@components/icons'
import { Card, Cards } from 'nextra/components'
import { getPagesUnderRoute } from 'nextra/context'

export function PSIndex() {
  return getPagesUnderRoute('/docs/guide/project_structures').map(page => {
    return (
      <Card icon={<FileIcon />} title={page?.meta.title} href={page.route} />
    )
  })
}

## 🫚 Project Structures

Before contributing to the project, you need to understand the project structure and how it works. This section guides you through the **basic project structure**.

<Cards>
  <PSIndex />
</Cards>
